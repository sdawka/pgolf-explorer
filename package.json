{
  "name": "pgolf-explorer",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name=pgolf-explorer --branch=main"
  },
  "dependencies": {
    "@astrojs/vue": "^5.0.0",
    "astro": "^5.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}